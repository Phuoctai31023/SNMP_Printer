<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <title>Quản lý Bộ Phận</title>

    <!-- Bootstrap & Icons -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css"
      rel="stylesheet"
    />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
      rel="stylesheet"
    />

    <!-- Google Fonts -->
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap"
      rel="stylesheet"
    />

    <!-- SweetAlert2 -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <style>
      body {
        font-family: "Roboto", sans-serif;
        background-color: #f5f6fa;
      }
      .card-header {
        font-weight: 500;
      }
      .overlay {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: rgba(255, 255, 255, 0.8);
        z-index: 9999;
        display: none;
        justify-content: center;
        align-items: center;
      }
      th {
        cursor: pointer;
        user-select: none;
      }
      th i {
        margin-left: 5px;
        font-size: 0.9em;
      }
      th.active-sort {
        background-color: #ffc107 !important;
        color: #000 !important;
      }
      td.highlight {
        background-color: #fff3cd !important;
      }
    </style>
  </head>
  <body>
    <!-- Loading Overlay -->
    <div class="overlay" id="loadingOverlay">
      <div
        class="spinner-border text-primary"
        style="width: 4rem; height: 4rem"
        role="status"
      >
        <span class="visually-hidden">Đang xử lý...</span>
      </div>
    </div>

    <div class="container mt-4">
      <!-- Header -->
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="text-primary">Xin chào, <%= user.username %> 👋</h2>
        <div class="dropdown">
          <button
            class="btn btn-outline-dark dropdown-toggle"
            data-bs-toggle="dropdown"
          >
            <i class="bi bi-person-circle"></i> <%= user.username %> (<%=
            user.role %>)
          </button>
          <ul class="dropdown-menu dropdown-menu-end">
            <% if (user && user.role === 'admin') { %>
            <li>
              <a class="dropdown-item" href="/printers">
                <i class="bi bi-printer me-2"></i>Quản lý máy in
              </a>
            </li>
            <li>
              <a class="dropdown-item" href="/users">
                <i class="bi bi-people me-2"></i>Quản lý người dùng
              </a>
            </li>
            <% } %>
            <li>
              <a class="dropdown-item" href="/change-password">
                <i class="bi bi-key me-2"></i>Đổi mật khẩu
              </a>
            </li>
            <li>
              <a class="dropdown-item text-danger" href="/logout">
                <i class="bi bi-box-arrow-right me-2"></i>Đăng xuất
              </a>
            </li>
          </ul>
        </div>
      </div>

      <!-- Thống kê nhanh -->
      <div class="row mb-4">
        <div class="col-md-3">
          <div class="card shadow-sm border-0">
            <div class="card-body text-center">
              <h6 class="card-title text-muted">Tổng số bộ phận</h6>
              <h3><%= departments ? departments.length : 0 %></h3>
            </div>
          </div>
        </div>
      </div>

      <!-- Form thêm bộ phận (chỉ admin) -->
      <% if (user && user.role === 'admin') { %>
      <div class="card shadow-sm mb-4">
        <div class="card-header bg-success text-white">
          <i class="bi bi-building-add"></i> Thêm Bộ Phận
        </div>
        <div class="card-body">
          <form
            method="POST"
            action="/departments/add"
            class="row g-3"
            onsubmit="showLoading()"
          >
            <div class="col-md-5">
              <input
                name="name"
                class="form-control"
                placeholder="Tên bộ phận"
                required
              />
            </div>
            <div class="col-md-5">
              <input
                name="description"
                class="form-control"
                placeholder="Mô tả"
              />
            </div>
            <div class="col-md-2">
              <button type="submit" class="btn btn-success w-100">
                <i class="bi bi-plus-circle"></i> Thêm
              </button>
            </div>
          </form>
        </div>
      </div>
      <% } %>

      <!-- Bộ lọc & tìm kiếm -->
      <div class="card-body p-2">
        <div class="row g-3 mb-3">
          <div class="col-md-6">
            <input
              type="text"
              id="searchInput"
              class="form-control"
              placeholder="🔍 Tìm kiếm bộ phận"
              onkeyup="filterTable()"
            />
          </div>
          <div class="col-md-3">
            <!-- reserved for future filters if needed -->
          </div>
          <div class="col-md-3"></div>
        </div>
      </div>

      <!-- Phân trang (hiển thị số & controls) -->
      <div class="d-flex justify-content-between align-items-center mb-2 px-2">
        <div>
          Hiển thị <span id="currentRange">0-0</span> /
          <span id="totalItems">0</span> bộ phận
        </div>
        <nav>
          <ul class="pagination pagination-sm mb-0" id="pagination"></ul>
        </nav>
      </div>

      <!-- Bảng danh sách bộ phận -->
      <div class="card shadow-sm">
        <div class="card-header bg-dark text-white">
          <i class="bi bi-list-ul"></i> Danh Sách Bộ Phận
        </div>
        <div class="table-responsive">
          <table id="deptTable" class="table table-hover align-middle mb-0">
            <thead class="table-dark">
              <tr>
                <th onclick="sortTable(0,this)">
                  Tên bộ phận <i class="bi bi-arrow-down-up"></i>
                </th>
                <th onclick="sortTable(1,this)">
                  Mô tả <i class="bi bi-arrow-down-up"></i>
                </th>
                <th onclick="sortTable(2,this)">
                  Ngày tạo <i class="bi bi-arrow-down-up"></i>
                </th>
                <th onclick="sortTable(3,this)">
                  Ngày cập nhật <i class="bi bi-arrow-down-up"></i>
                </th>
                <th>Hành động</th>
              </tr>
            </thead>
            <tbody id="deptBody">
              <% if (Array.isArray(departments) && departments.length > 0) { %>
              <% departments.forEach(function(d){ %>
              <tr>
                <td>
                  <a
                    href="/departments/<%= d._id %>"
                    class="text-decoration-underline fw-normal text-primary"
                  >
                    <%= d.name %>
                  </a>
                </td>
                <td><%= d.description || '-' %></td>
                <td><%= d.createdAtFormatted %></td>
                <td><%= d.updatedAtFormatted %></td>
                <td>
                  <% if (user && user.role === 'admin') { %>
                  <button
                    class="btn btn-warning btn-sm me-1"
                    onclick="openEditModalFromBtn(this)"
                    data-id="<%= d._id %>"
                    data-name="<%= d.name %>"
                    data-description="<%= d.description %>"
                  >
                    <i class="bi bi-pencil-square"></i>
                  </button>

                  <form
                    method="POST"
                    action="/departments/delete/<%= d._id %>"
                    class="d-inline"
                    onsubmit="return confirm('Bạn có chắc muốn xóa bộ phận này?');"
                  >
                    <button class="btn btn-danger btn-sm">
                      <i class="bi bi-trash"></i>
                    </button>
                  </form>
                  <% } else { %>
                  <span class="text-muted">-</span>
                  <% } %>
                </td>
              </tr>
              <% }) %> <% } else { %>
              <tr>
                <td colspan="5" class="text-center text-muted">
                  Không có bộ phận
                </td>
              </tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>
    </div>

    <!-- Modal sửa -->
    <div class="modal fade" id="editModal" tabindex="-1" aria-hidden="true">
      <div class="modal-dialog">
        <form method="POST" id="editForm">
          <div class="modal-content">
            <div class="modal-header bg-warning text-dark">
              <h5 class="modal-title">
                <i class="bi bi-pencil-square"></i> Sửa bộ phận
              </h5>
              <button
                type="button"
                class="btn-close"
                data-bs-dismiss="modal"
              ></button>
            </div>
            <div class="modal-body">
              <input type="hidden" name="id" id="editId" />
              <div class="mb-3">
                <label class="form-label">Tên bộ phận</label>
                <input
                  type="text"
                  class="form-control"
                  name="name"
                  id="editName"
                  required
                />
              </div>
              <div class="mb-3">
                <label class="form-label">Mô tả</label>
                <input
                  type="text"
                  class="form-control"
                  name="description"
                  id="editDescription"
                />
              </div>
            </div>
            <div class="modal-footer">
              <button
                type="button"
                class="btn btn-secondary"
                data-bs-dismiss="modal"
              >
                Đóng
              </button>
              <button
                type="submit"
                class="btn btn-warning"
                onclick="showLoading()"
              >
                Lưu thay đổi
              </button>
            </div>
          </div>
        </form>
      </div>
    </div>

    <!-- Scripts -->
    <script>
      function showLoading() {
        const ov = document.getElementById("loadingOverlay");
        if (ov) ov.style.display = "flex";
      }

      // Alert hiển thị lỗi/thành công (giữ giống users.ejs)
      const params = new URLSearchParams(window.location.search);
      const error = params.get("error");
      const success = params.get("success");

      if (error) {
        Swal.fire({
          icon: "error",
          title: "Lỗi",
          text: decodeURIComponent(error),
          confirmButtonColor: "#d33",
        }).then(() =>
          window.history.replaceState(
            {},
            document.title,
            window.location.pathname
          )
        );
      }
      if (success) {
        Swal.fire({
          icon: "success",
          title: "Thành công",
          text: decodeURIComponent(success),
          confirmButtonColor: "#28a745",
        }).then(() =>
          window.history.replaceState(
            {},
            document.title,
            window.location.pathname
          )
        );
      }

      // Sort table (giống users.ejs)
      let sortDirections = {};
      function sortTable(colIndex, headerEl) {
        const table = document.getElementById("deptTable");
        const tbody = table.querySelector("tbody");
        const rows = Array.from(tbody.querySelectorAll("tr")).filter((r) =>
          r.querySelector("td")
        );
        sortDirections[colIndex] = !sortDirections[colIndex];
        rows.sort((a, b) => {
          let cellA = a.children[colIndex].innerText.trim();
          let cellB = b.children[colIndex].innerText.trim();
          return sortDirections[colIndex]
            ? cellA.localeCompare(cellB, "vi", { sensitivity: "base" })
            : cellB.localeCompare(cellA, "vi", { sensitivity: "base" });
        });
        tbody.innerHTML = "";
        rows.forEach((r) => tbody.appendChild(r));

        document
          .querySelectorAll("th")
          .forEach((th) => th.classList.remove("active-sort"));
        document
          .querySelectorAll("td")
          .forEach((td) => td.classList.remove("highlight"));
        document
          .querySelectorAll("th i")
          .forEach((icon) => (icon.className = "bi bi-arrow-down-up"));

        const icon = headerEl.querySelector("i");
        icon.className = sortDirections[colIndex]
          ? "bi bi-arrow-up"
          : "bi bi-arrow-down";
        headerEl.classList.add("active-sort");

        tbody.querySelectorAll("tr").forEach((row) => {
          row.children[colIndex] &&
            row.children[colIndex].classList.add("highlight");
        });
      }

      // Filter + pagination logic (same structure as users.ejs)
      let originalRows = [];
      let currentPage = 1;
      let rowsPerPage = 10;

      function filterTable() {
        let search = document.getElementById("searchInput").value.toLowerCase();

        let filtered = originalRows.filter((row) => {
          if (!row.querySelector("td")) return false;
          let cells = row.getElementsByTagName("td");
          let name = (cells[0].innerText || "").toLowerCase();
          let desc = (cells[1].innerText || "").toLowerCase();

          let matchesSearch = name.includes(search) || desc.includes(search);

          return matchesSearch;
        });

        renderTable(filtered);
      }

      function renderTable(filtered) {
        let tbody = document.getElementById("deptBody");
        document.getElementById("totalItems").innerText = filtered.length;

        let totalPages = Math.ceil(filtered.length / rowsPerPage) || 1;
        if (currentPage > totalPages) currentPage = 1;
        let start = (currentPage - 1) * rowsPerPage;
        let end = start + rowsPerPage;

        document.getElementById("currentRange").innerText =
          filtered.length > 0
            ? `${start + 1}-${Math.min(end, filtered.length)}`
            : "0-0";

        tbody.innerHTML = "";
        if (filtered.length === 0) {
          tbody.innerHTML = `<tr><td colspan="5" class="text-center text-muted">Không có bộ phận</td></tr>`;
        } else {
          filtered.slice(start, end).forEach((row) => tbody.appendChild(row));
        }

        renderPagination(totalPages);
      }

      function renderPagination(totalPages) {
        let pagination = document.getElementById("pagination");
        pagination.innerHTML = "";

        for (let i = 1; i <= totalPages; i++) {
          let li = document.createElement("li");
          li.className = "page-item" + (i === currentPage ? " active" : "");
          let a = document.createElement("a");
          a.className = "page-link";
          a.href = "#";
          a.innerText = i;
          a.onclick = function (e) {
            e.preventDefault();
            currentPage = i;
            filterTable();
          };
          li.appendChild(a);
          pagination.appendChild(li);
        }
      }

      // Mở modal sửa bộ phận
      function openEditModalFromBtn(btn) {
        document.getElementById("editId").value = btn.dataset.id;
        document.getElementById("editName").value = btn.dataset.name;
        document.getElementById("editDescription").value =
          btn.dataset.description || "";
        document.getElementById("editForm").action =
          "/departments/update/" + btn.dataset.id;
        new bootstrap.Modal(document.getElementById("editModal")).show();
      }

      window.onload = function () {
        originalRows = Array.from(
          document.querySelectorAll("#deptBody tr")
        ).filter((r) => r.querySelector("td"));
        filterTable();
      };
    </script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
